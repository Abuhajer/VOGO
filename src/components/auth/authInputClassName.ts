/** Shared auth field styling — always readable on dark auth surfaces. */
export const authInputClassName =
  "w-full rounded-sm border border-gold-glow/25 bg-surface px-4 py-3.5 text-sm text-ivory placeholder:text-ivory-faint focus:border-gold/50 focus:outline-none focus:ring-1 focus:ring-gold/30 transition-colors";

export const authPasswordInputClassName = `${authInputClassName} pe-12`;
