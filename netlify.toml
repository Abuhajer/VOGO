[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
  # Set DATABASE_URL, AUTH_SECRET, and optional Stripe/analytics keys in Netlify UI.

[[plugins]]
  package = "@netlify/plugin-nextjs"
